<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <%= link_to "nowmenu", root_path, class: "navbar-brand"  %>
  <span class="navbar-toggler"  data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <i class="fas fa-user"></i>
  </span>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">

    </ul>
    <% if user_signed_in? %>
      <li class="nav-item dropdown">
          <span class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
              <i class="fas fa-user"><% current_user.email %></i>
          </span>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <div class="dropdown-item">
              <%= current_user.email %>
            </div>
            <div class="dropdown-divider"></div>
            <div class="dropdown-item">
              <%= link_to "logout", destroy_user_session_path,  method: :delete, class: "btn btn-sm btn-danger", data: { confirm: "Are you sure?"} %>
            </div>
          </div>
      </li>
    <% else %>
        <li class="nav-item"><%= link_to "sign in", new_user_session_path %></li>
    <% end %>
  </div>
</nav>
