
<nav class="navbar navbar-expand-xl navbar-light sticky-top">

  <%= link_to home_path do %>

    <%= image_tag 'task101', class: 'img-fluid' %>

  <% end %>

  <div class=" navbar-nav mx-auto  text-left">
    <% if user_signed_in? %>
      <%= form_with url: search_path, local: true, method: :get, html: { class: "form-inline  my-lg-0" } do |form| %>
      <%= form.text_field :q, placeholder: "enter your search term here", data: { behavior: "autocomplete" }, class: "form-control search-field" %>
      <%= form.button "find", class: "btn btn-sm btn-outline-secondary my-2 my-sm-0 ml-3" %>
    <% end %>

  </div>

    <li class="nav-item dropdown">
      <span class="nav-link dropdown-toggle" href="#" id="navbarDropdown" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
        <i class="fas fa-user-circle"></i>
      </span>
      <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
        <div class="card p-2 border-0">
          <div class="dropdown-item">
            <small><%= link_to current_user.email, edit_user_registration_path %></small>
          </div>

          <div class="dropdown-item">
            <small><%= link_to "logout", destroy_user_session_path,  method: :delete, data: { confirm: "Are you sure?"} %></small>
          </div>
        </div>
      </div>
    </li>
  <% else %>
      <small><%= link_to "sign in", new_user_session_path %></small>
  <% end %>

</nav>
<br />
